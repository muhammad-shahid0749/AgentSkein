# Copy to .env and fill in your values

# Redis connection (required for Redis backend)
REDIS_URL=redis://localhost:6379/0

# Optional: OpenAI API key (for MERGE_SEMANTIC strategy)
# OPENAI_API_KEY=sk-...

# Optional: Anthropic API key (for MERGE_SEMANTIC strategy)
# ANTHROPIC_API_KEY=sk-ant-...
